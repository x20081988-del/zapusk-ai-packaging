{
  "name": "zapusk-ai-packaging",
  "private": true,
  "version": "0.1.0",
  "description": "Zapusk AI Packaging — investment packaging cockpit (MVP)",
  "engines": {
    "node": ">=22.22.0 <23"
  },
  "scripts": {
    "install:all": "npm install --include=dev && npm --prefix server install --include=dev && npm --prefix web install --include=dev",
    "dev": "concurrently -k -n api,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix server run build && npm --prefix web run build",
    "start": "npm --prefix server run start:prod",
    "db:migrate": "npm --prefix server run db:migrate",
    "db:seed": "npm --prefix server run db:seed",
    "db:reset": "npm --prefix server run db:reset",
    "db:doctor": "npm --prefix server run db:doctor",
    "env:doctor": "npm --prefix server run env:doctor",
    "maintenance:disk": "npm --prefix server run maintenance:disk",
    "smoke:transcript": "node scripts/transcript-aggregation-smoke.mjs",
    "smoke:project-knowledge": "npx --prefix server tsx scripts/project-knowledge-smoke.ts",
    "benchmark:retrieval": "npx --prefix server tsx server/src/scripts/retrievalBenchmark.ts",
    "stress:ingest": "npx --prefix server tsx server/src/scripts/ingestStress.ts",
    "kb:backfill": "npx --prefix server tsx server/src/scripts/kbBackfill.ts",
    "audit:token-budget": "npx --prefix server tsx server/src/scripts/tokenBudgetAudit.ts",
    "audit:duplicate-ingest": "npx --prefix server tsx server/src/scripts/duplicateIngestAudit.ts",
    "regression:realcalls": "node scripts/real-call-regression.mjs",
    "replay": "node scripts/pipeline-replay.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
