node_modules
dist
build
.env
.env.local
.env.*
!.env.example
server/.env.*
!server/.env.example
web/.env.*
!web/.env.example
*.log
.DS_Store

# server
server/uploads
# Sprint 61.P1 — stress/benchmark scripts run with cwd=project root and
# create ./uploads. Эфемерное, не комитим.
/uploads
server/prisma/*.db
server/prisma/*.db-journal
server/prisma/migrations/dev.db
# Sprint 59 P0.7 — audio fixture library. PII-sensitive + large. Only
# INDEX.md is tracked; .wav and ground-truth transcripts stay local.
server/test-fixtures/audio/**/*.wav
server/test-fixtures/audio/**/*.mp3
server/test-fixtures/audio/**/*.webm
server/test-fixtures/audio/**/*.m4a
server/test-fixtures/audio/**/*.ground.txt
server/test-fixtures/audio/**/*.meta.json
server/test-fixtures/audio/manifest.json

# web
web/dist

# Sprint 62.P16 — Owner-Mode real signals export. Generated locally by the
# external TG-BOT + Outreach project; must never be committed (real names,
# @handles, t.me links, chat titles). Safe Demo Mode uses the committed
# signals.demo.json instead.
web/public/signals.import.json

# stale tsc-emit (Vite uses .tsx; .js next to .tsx shadows imports)
web/src/**/*.js
web/tsconfig.tsbuildinfo

# Рабочие артефакты supergoal-планов: планы, состояние, доказательства прогонов.
# В репозиторий не уходят - это заметки процесса, а не код.
.supergoal/
